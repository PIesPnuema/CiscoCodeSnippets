{
	// Place your snippets for platformio-debug.asm here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }


	"x86 asm": {
        "prefix": "aRegistersAnd operators",
        "scope": "assembly",
        "body": [
            "// General Purpose registers",
            "eax == Accumulator",
            "ecx == Counter",
            "edx == Data",
            "ebx == Base ",
            " ",
			"// Pointer and Index registers",
			"esp == Stack pointer",
			"ebp == Base pointer",
			"esi == Source index ",
			"edi == destination index",
			"",
			"eip == instruction pointer register",
			"",
			"eflags == Used for code segmentation",
			" ",
			"// Operators",
			"cmp == compare",
			"",
        ],
        "description": "Prints x86 register meanings"
    },

}